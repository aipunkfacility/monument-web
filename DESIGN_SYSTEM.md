# Дизайн-система Monument Web (v2.0)

Это руководство описывает визуальный язык и техническую реализацию дизайн-системы, используемой в проекте `monument-web`. Система построена на принципах **Glassmorphism**, модульной сетки и адаптивной типографики.

---

## 📐 Сетка и Отступы (8-Point Grid)

В системе используется 8-пиксельная сетка для обеспечения визуального ритма и выравнивания. Все значения выражены в `rem` для корректного масштабирования.

| Токен | Значение (px) | Применение |
| :--- | :--- | :--- |
| `--space-1` | 4px | Микро-отступы, тонкая настройка |
| `--space-2` | 8px | Гэпы в иконках, мелкие внутренние отступы |
| `--space-3` | 12px | Отступы в списках, карточках |
| `--space-4` | 16px | Стандартный отступ, мобильные контейнеры |
| `--space-5` | 20px | Десктопные контейнеры, логотипы |
| `--space-6` | 24px | Внутренние отступы крупных карточек |
| `--space-8` | 32px | Гэпы в гридах (Advantages, Pricing) |
| `--space-10`| 40px | Отступы в CTA, крупные компоненты |
| `--space-12`| 48px | Вертикальный ритм на мобильных |
| `--space-16`| 64px | Вертикальный отступ секций (Desktop) |

---

## ✍️ Типографика

Система использует два основных шрифта: **Outfit** для акцентов и **Inter** для текста.

### Шрифтовые пары
- **Заголовки (H1-H6):** `Outfit`, 600-700 weight.
- **Текст (Body):** `Inter`, 400-500 weight.

### Флюидная шкала (Fluid Type)
Заголовки автоматически масштабируются в зависимости от ширины вьюпорта с помощью `clamp()`.

| Токен | Базовое значение (Mobile) | Макс. значение (Desktop) | Применение |
| :--- | :--- | :--- | :--- |
| `--text-4xl` | 40px | 72px | Главный заголовок (H1) |
| `--text-3xl` | 32px | 48px | Заголовки разделов (H2) |
| `--text-2xl` | 24px | 24px | Подзаголовки (H3) |
| `--text-base` | 16px | 16px | Основной текст |
| `--text-sm`   | 14px | 14px | Мелкие примечания, футер |

---

## 🎨 Цветовая палитра (Семантическая)

Цвета разделены на семантические группы для автоматической поддержки Темной и Светлой темы.

### Темная тема (Dark Mode - Default)
- **Background:** `#1a1a2e` (Primary), Deep gradients.
- **Surface:** `rgba(255, 255, 255, 0.08)` (Semi-transparent).
- **Text:** `#ffffff` (Headings), `#e0e0e0` (Body).
- **Accent:** `#3498db` (Blue).

### Светлая тема (Light Mode)
- **Background:** `#f0f2f5` (Neutral Gray/White).
- **Surface:** `rgba(255, 255, 255, 0.75)` (Blurred Glass).
- **Text:** `#1a1a2e` (Headings), `#333333` (Body).
- **Accent:** `#2980b9` (Deep Blue).

---

## 🧊 Эффекты Glassmorphism

Единый визуальный стандарт для всех «стеклянных» элементов.

- **Размытие:** `--blur-glass: 16px`
- **Тени (Dark):** `0 8px 32px rgba(0, 0, 0, 0.3)`
- **Тени (Light):** `0 8px 32px rgba(0, 0, 0, 0.08)`
- **Границы:** 1px `var(--surface-border)`

---

## 🔘 Компоненты

### Кнопки (Buttons)
- **Primary:** Фоновая заливка акцентным цветом, белый текст.
- **Outline:** Прозрачный фон, акцентная рамка и текст.
- **Стандартный радиус:** `--radius-md` (12px).
- **Padding:** `var(--space-4)` / `var(--space-8)`.

### Карточки (Cards)
- **Фон:** `var(--surface-bg)`.
- **Бордюр:** `1px solid var(--surface-border)`.
- **Радиус:** `--radius-lg` (20px).

---

## 📱 Адаптивность

- **Desktop:** Контейнер 1200px, Padding 20px.
- **Mobile (<=768px):** Padding 16px, Секции 48px.
- **Хедер:** Фиксированная высота 72px (9 * 8px).

---

## 🛠 Реализация в коде

Все токены определены в `:root` и `[data-theme="light"]` внутри `css/style.css`.
При добавлении новых элементов **обязательно** используйте переменные `var(--space-*)` и `var(--text-*)` вместо фиксированных значений.
